# Optional. Leave empty to use the built-in diagnostic globe and graticule.
# Optional. Defaults to /maps/geomuse-basemap.pmtiles.
# Remote sources must use HTTPS, end in .pmtiles, and contain no credentials,
# query string, or fragment. Prefer immutable versioned file names.
# Example: https://static.example.com/maps/geomuse-basemap-v1.pmtiles
NEXT_PUBLIC_BASEMAP_PMTILES_URL=
